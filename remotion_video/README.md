# Video Overview

This folder contains the Remotion source for the project overview video.

```bash
npm install
npm run preview
npm run render
```

Rendered MP4 files should stay outside Git history unless a short, sanitized
final asset is intentionally added. For the GitHub repository overview page,
upload the final MP4 to GitHub Releases or GitHub user attachments and link it
from the README with a static thumbnail.
